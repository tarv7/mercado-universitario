<h2 class="title_orders"><%= I18n.t('order.title.orders_made') %></h2>
<hr>
<% @orders.each do |order| %>
  <%= render 'card_order', order: order %>
<% end %>

<%# @orders.each do |order| %>
  <p><%#= "#{order.order_products.first.product.seller.name} ---> #{order.status} ---> R$#{order.price}" %></p>
  <%# binding.pry %>
<%# end %>
